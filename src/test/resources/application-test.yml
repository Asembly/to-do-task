spring:
  application:
    name: app
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5433/db
    password: pass
    username: postgres
  jwt:
    secret: mysecretkey
    expiration: 3600000